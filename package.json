{
  "name": "sports-meet",
  "version": "1.0.0",
  "private": true,
  "description": "Blue Star Sports Club — full-stack app (React + Express + MongoDB) on Vercel",
  "scripts": {
    "vercel-build": "cd client && npm install --legacy-peer-deps && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^2.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0",
    "multer": "^2.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
