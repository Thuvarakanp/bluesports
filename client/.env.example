# Optional. Leave EMPTY for the all-in-one Vercel deployment (frontend and API
# share the same domain, so relative "/api" calls just work).
#
# Only set this if you host the backend on a SEPARATE domain, e.g.:
# VITE_API_URL=https://your-backend.example.com
VITE_API_URL=
