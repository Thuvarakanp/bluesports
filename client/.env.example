# URL of the deployed backend API (Render).
# Leave this UNSET for local development — the Vite dev proxy handles /api itself.
# In Vercel, set this in Project Settings -> Environment Variables.
# Example:
# VITE_API_URL=https://sports-meet-server.onrender.com
VITE_API_URL=
